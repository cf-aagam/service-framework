query SearchSkuByCode($searchQuery: SearchQuery!) {
    searchSkus(searchQuery: $searchQuery) {
        attributes {
            ... on AttributeWithScalarValue {
                value
                attribute {
                    name
                }
            }
        }
    }
}
